<% layout('layouts/boilerplate') %>
<link rel="stylesheet" href="/stylesheets/stars.css">

<div class="row">
    <div class="col-8 offset-2">
        <% if (data.file) { %>
            
            <object data="/upload/<%= data.file.filename %>" type="application/pdf" width="100%" height="500px">
                <p>Unable to display PDF file. <a href="/upload/<%= data.file.filename %>">Download</a> instead.</p>
            </object> 
        <% } %>
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">
                    <%= data.paper.title %>
                </h5>
                <p class="card-text">
                    <%= data.paper.description %>
                </p>
            </div>
            <% if (currentUser && (data.paper.author.equals(currentUser._id))) { %>
                <div class="card-body">
                    <a class="card-link btn btn-info text-white"
                        href="/papers/<%= data.paper._id %>/edit">Edit</a>
                    <form class="d-inline" action="/papers/<%= data.paper._id %>?_method=DELETE" method="post">
                        <button class="btn btn-danger">Delete</button>
                    </form>
                </div>
            <% } %>
            <div class="card-footer text-muted">
                <a href="/papers">All Papers</a>
            </div>
        </div>
    </div>
    <% if (currentUser) { %>
        <h2>Leave a Suggestion</h2>
            <div class="col-6">
                <form action="/papers/<%= data.paper._id %>/suggestion" class="mb-3 validated-form" novalidate
                    method="post">
                    <div class="mb-3">
                        <label class="form-label" for="body">Suggestion</label>
                        <textarea class="form-control" name="suggestion[body]" id="body" cols="30" rows="4"
                            required></textarea>
                        <div class="valid-feedback">
                            Looks Good!!
                        </div>
                        <div class="invalid-feedback">
                            Give Description
                        </div>
                    </div>
                    <button class='btn btn-success'>Submit</button>
                </form>
            </div>
    <% } %>
    <div class="col-6">
        <% for(let suggestion of data.paper.suggestions) {%>
            <div class="card mb-3">
                <div class="card-body">
                    <h5 class="card-title">
                        <%= suggestion.author.username %>
                    </h5>
                    <p class="card-text">Suggestion: <%= suggestion.body %>
                    </p>
                    <% if (currentUser && (suggestion.author.equals(currentUser._id))) { %>
                        <form
                            action="/papers/<%= suggestion._id %>/suggestion/<%= suggestion._id %>?_method=DELETE"
                            method="post">
                            <button class="btn btn-sm btn-danger">Delete</button>
                        </form>
                    <% } %>
                </div>
            </div>
        <% } %>
    </div>
</div>

<script>
    const paper = <%- JSON.stringify(data.paper) %>;
</script>